version: '3.8'

services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8043:8043"
    environment:
      - SPIDERFOOT_API_KEY=${SPIDERFOOT_API_KEY}
      - SPIDERFOOT_BASE_URL=http://127.0.0.1:5001
      - ENV=prod


